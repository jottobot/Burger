<h1>Eat Da Burger</h1>

<h2>On the menu...</h2>

<ul>
  {{#each burgers}}
  {{#unless devoured}}

  <form action="burger/eat/{{id}}" method="POST">
    <div class="input-group">
      <li class="form-control">{{id}}. {{burger_name}}</li>
      <span class="input-group-btn">
        <button class="btn btn-success devour-button" type="submit">Devour It!</button>
      </span>
    </div>
  </form>
  {{!-- {{> burgers/burger-block devour=true}} --}}
  {{/unless}}
  {{/each}}
</ul>

<h2>Devoured Burgers</h2>

<ul>
  {{#each burgers}}
  {{#if devoured}}
  <div class="input-group">
    <li class="form-control">{{this.id}}. {{this.burger_name}}</li>
    <span class="input-group-btn">
      <button class="btn btn-info whine-button" type="button">Whine!</button>
    </span>
  </div>
  {{!-- {{> burgers/burger-block devour=false}} --}}
  {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="newburger" name="name">
  </div>

  {{!-- <div class = "form-group">
        <label for="devoured">Devoured yet?</label><br>
        <input type="radio" name="devoured" value="0" checked>Not quite...<br>
        <input type="radio" name="devoured" value="1">Devoured!
    </div> --}}

  <button type="submit">Add Burger</button>
</form>