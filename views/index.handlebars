<h1>Eat Da Burger</h1>
<img src="assets/img/1494459418304.jpeg" height="200px"></img>

{{!-- <div class="container">
  <div class="row">
    <div class="col-md-6"> --}}
      <h2>On the menu...</h2>
      <ul>
        {{#each burgers}}
        {{#if devoured}}
        {{> burgers/burgers-block devour=false}}
        {{/if}}
        {{/each}}
      </ul>
    {{!-- </div> --}}

    {{!-- <div class="col-md-6"> --}}
      <h2>Devoured Burgers</h2>
      <ul>
        {{#each burgers}}
        {{#unless devoured}}
        {{> burgers/burgers-block devour=true}}
        {{/unless}}
        {{/each}}
      </ul>
    {{!-- </div> --}}

  {{!-- </div> --}}

  {{!-- <div class="row"> --}}
    <h2>Add a Burger</h2>
    <form class="create-form">
      <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="newburger" name="name">
      </div>

      <button type="submit">Add Burger</button>
    </form>
  {{!-- </div>

</div> --}}